name: CI

on: [push, pull_request]

jobs:
  build:
    strategy:
      matrix:
        platform: [ubuntu-latest, macos-latest]
    runs-on: ${{ matrix.platform }}
    steps:
    - uses: actions/checkout@v2
    - name: Install bats
      run: |
        [[ "$(uname)" == "Darwin" ]] && brew install bats
        [[ "$(uname)" == "Linux" ]] && sudo apt-get install bats
    - name: Run test
      run: make test
