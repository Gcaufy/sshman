name: CI

on: [push]

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v2
    - name: Install bats
      run: sudo apt-get install bats
    - name: Run test
      run: make test
